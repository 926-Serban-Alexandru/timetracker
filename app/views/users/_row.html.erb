<tr id="<%= dom_id(user) %>">
  <td>
    <% if current_user.admin? %>
    <%= link_to user.name, time_entries_path(user_id: user.id), class: "text-dark" %>
    <% else %>
      <%= user.name %>
    <% end %>
  </td>
  <td><%= user.email %></td>
  <td>
    <div class="d-flex justify-content-center gap-2">
      <%= link_to "Edit", users_path(edit_id: user.id), class: "btn btn-sm btn-warning text-dark" %>
      <%= button_to "Delete", user_path(user), method: :delete,
            data: { turbo_confirm: "Are you sure?" },
            class: "btn btn-sm btn-danger text-white" %>
    </div>
  </td>
</tr>




